# 贡献指南

[English](CONTRIBUTING.md)

感谢你想要贡献。本列表旨在真正实用，因此关于添加内容有一些严格的规则。

## 添加提供商

在提交 PR 添加新提供商之前，请确保它满足**所有**以下要求：

1. **真正免费。** 不需要信用卡，不会自动扣费的试用，没有"免费但需要先添加付款方式"。如果用户必须输入付款信息才能访问免费层级，那么它不属于这里。

2. **提供 API 访问。** 网页界面、聊天机器人和"在浏览器中试用"的页面不算。提供商必须通过 API（REST、WebSocket 或类似方式）提供编程式访问。如果 CLI 工具可以编写脚本，则也算。

3. **目前可用。** 在提交之前请自行测试。进行一次实际的 API 调用。如果提供商已下线、返回错误，或免费层级已不可用，请不要提交。

4. **模型实用。** 免费模型应该能够处理实际任务。一个几乎无法组成完整句子的 5 亿参数模型不算。模型应该能够以合理的质量水平编写代码、回答问题或执行有用的任务。

5. **不接受虚假提供商。** 不要提交你本人未亲自测试过的提供商。不要提交需要通过推荐链接注册才能访问的提供商。不要提交托管在没有文档的可疑域名上的提供商。

## 以下情况会导致你的 PR 被拒绝

- **虚假或未经验证的提供商。** 如果你无法证明它可用，就不会被添加。
- **死链接。** 如果注册页面、API 文档或端点已下线，就不会被添加。
- **需要付款信息才能免费使用的提供商。** 这是一票否决的。没有例外。
- **只提供聊天界面的提供商。** 没有 API 就不能入选。
- **免费模型不可用的提供商。** 如果免费层级只提供一个无法完成基本编码任务的模型，那对本列表来说不够实用。
- **托管在明显临时基础设施上的提供商。** 如果域名是 `free-llm-api-2024.herokuapp.com`，那它不会长久。
- **复制粘贴的提交。** 如果你的提供商描述是从提供商的营销页面复制的，没有任何实际测试或个人体验，将会被移除。

## PR 格式

添加提供商时，请遵循以下格式：

```markdown
### 提供商名称

| 项目 | 信息 |
|------|------|
| **模型** | 模型名称 |
| **API 端点** | API 基础 URL |
| **注册** | [注册链接](https://example.com) |
| **认证** | 认证方式 |
| **速率限制** | 已知的速率限制 |
| **需要信用卡** | 需要 / 不需要 |
| **备注** | 任何重要细节 |

提供商的描述、使用方法以及它为什么有用。具体说明免费层级的限制。包括充分利用它的提示。
```

同时在中英文 README 的快速对比表中添加一行。

## 移除提供商

如果某个提供商的免费层级不再可用，请提交 issue 或 PR，并提供证据（截图、错误日志或更新的条款页面）。该提供商将被移除或标记为不可用。

## 翻译

所有文档文件（README、CONTRIBUTING、CODE_OF_CONDUCT）都必须有中文翻译。更新英文版本时，请同时更新中文版本。在语言版本之间添加交叉链接。

## 有疑问？

如果不确定某个提供商是否符合条件，请在提交 PR 之前先开一个 issue。